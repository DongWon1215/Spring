<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/mvc
       https://www.springframework.org/schema/mvc/spring-mvc.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd">

    <mvc:resources mapping="/todoList/**" location="/WEB-INF/todoList/"></mvc:resources>
    <mvc:annotation-driven></mvc:annotation-driven>
    <mvc:resources mapping="/uploadfile/**" location="/uploadfile/"/>
    <context:component-scan base-package="todoList"></context:component-scan>

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/todoList/"></property>
        <property name="suffix" value=".jsp"></property>
    </bean>

    <bean id="Conn" class="todoList.Util.ConnectionProvider" scope="singleton"></bean>

    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

    <!--회원 로그인 여부 체크 interceptor 등록-->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/page/**"/>
            <bean class="todoList.Interceptor.LoginCheckInterceptor"/>

        </mvc:interceptor>
    </mvc:interceptors>

    <bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
        <property name="formatters">
            <set>
                <bean class="todoList.formatter.LocalDateFormatter"/>
            </set>
        </property>
    </bean>

</beans>

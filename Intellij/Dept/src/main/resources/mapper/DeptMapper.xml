<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dept.manager.mapper.DeptMapper">
    <select id = "selectAll" resultType="dept.manager.domain.DeptDTO">select * from dept
    </select>

    <select id = "selectByDeptno" resultType="dept.manager.domain.DeptDTO">select * from dept where deptno = #{deptno}</select> <!--#{}안에 들어가는 변수의 이름은 변수가 1개일땐 아무 이름이나 들어가도 상관 없지만 2개 이상일 경우 구분을 해줘야 한다-->

    <update id="updateDept" parameterType="dept.manager.domain.DeptDTO">update dept set dname = #{dname}, loc = #{loc}, where deptno = #{deptno}</update><!--객체 형식으로 들어왔기 때문에 객체의 변수 명으로 작성해도 알아서 매핑해준다-->

    <insert id="insertDept" parameterType="dept.manager.domain.DeptDTO">insert into dept values(#{deptno},#{dname},#{loc})</insert>
</mapper>